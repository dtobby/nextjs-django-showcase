from django.urls import path
from . import views # Import views later when you create them

urlpatterns = [
    # Add product-specific URL patterns here later
    # e.g., path('', views.product_list, name='product-list'),
]